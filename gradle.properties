# تنظیمات Gradle — اپ اندروید سهند سرویس
org.gradle.jvmargs=-Xmx1280m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=false
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=false
# v2.11.1 — JDK با کامپایلر (سیستم فقط JRE دارد)
# v2.11.5 — رزولوشن toolchain فقط از jdk-merged (سیستم JRE است)
# v2.12.1 — JDK با کامپایلر (سیستم فقط JRE دارد)
# v2.12.1 — JDK با کامپایلر (سیستم فقط JRE دارد) — gradlew سفارشی java را از PATH می‌گیرد
org.gradle.java.installations.paths=/home/z/my-project/tools/jdk-17.0.20.1+1
